# Backend-only placeholders. Never commit real values and never expose these through Vite.
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are supplied by the hosted Edge runtime.
# For local functions, use local CLI credentials only; see docs/DEPLOYMENT.md.
SUPABASE_SERVICE_ROLE_KEY=
NUEDE_WHATSAPP_NUMBER=your-international-number
PAYSTACK_SECRET_KEY=
# Final storefront origin for provider callbacks. Keep Paystack test/live modes consistent.
NUEDE_STOREFRONT_URL=https://your-storefront.example/
