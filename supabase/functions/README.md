# Edge Functions

Supabase Edge Functions will eventually own trusted order validation, price calculation, payment initialization, WhatsApp order creation, payment verification, and webhook processing.

No Edge Function is implemented during Cycle 0. Secrets used here are backend-only and must never be exposed through a `VITE_` variable or imported into either application.
