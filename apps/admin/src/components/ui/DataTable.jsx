export function DataTable({ caption, columns, rows, renderRow }) {
  return <div className="overflow-hidden rounded-card border border-line bg-surface"><div className="max-w-full overflow-x-auto" tabIndex="0" role="region" aria-label={`${caption}. Scroll horizontally on narrow screens.`}><table className="w-full min-w-[44rem] border-collapse text-left"><caption className="sr-only">{caption}</caption><thead className="border-b border-line bg-canvas"><tr>{columns.map((column) => <th key={column} scope="col" className="px-4 py-3 text-[0.68rem] font-bold uppercase tracking-[0.14em] text-muted">{column}</th>)}</tr></thead><tbody className="divide-y divide-line">{rows.map((row, index) => renderRow(row, index))}</tbody></table></div></div>;
}
