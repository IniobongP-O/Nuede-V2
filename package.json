{
  "name": "nuede-v2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Nuede V2 storefront, admin application, shared packages, and Supabase backend workspace.",
  "packageManager": "npm@11.17.0",
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11 <12"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:storefront": "npm run dev --workspace=@nuede/storefront",
    "dev:admin": "npm run dev --workspace=@nuede/admin",
    "build:storefront": "npm run build --workspace=@nuede/storefront",
    "build:admin": "npm run build --workspace=@nuede/admin",
    "build": "npm run build:storefront && npm run build:admin",
    "lint": "eslint . --max-warnings=0",
    "test": "node --test scripts/*.test.js",
    "verify": "npm run lint && npm test && npm run build",
    "supabase:start": "supabase start",
    "supabase:stop": "supabase stop",
    "db:reset": "supabase db reset --local",
    "db:test": "supabase test db --local",
    "verify:cycle2": "npm run db:reset && npm run db:test && npm run verify",
    "test:security:cycle3": "node scripts/verify-cycle3-security.mjs",
    "verify:cycle3": "npm run db:reset && npm run db:test && npm run test:security:cycle3 && npm run verify",
    "test:catalog:cycle4": "node scripts/verify-cycle4-catalog.mjs",
    "verify:cycle4": "npm run db:reset && npm run db:test && npm run test:security:cycle3 && npm run test:catalog:cycle4 && npm run verify",
    "test:catalog:cycle5": "node scripts/verify-cycle5-catalog.mjs",
    "verify:cycle5": "npm run db:reset && npm run db:test && npm run test:security:cycle3 && npm run test:catalog:cycle4 && npm run test:catalog:cycle5 && npm run verify",
    "test:browser:cycle18": "node scripts/verify-cycle18-browser.mjs",
    "test:secrets": "node scripts/verify-cycle18-secrets.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@electric-sql/pglite": "^0.5.8",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "eslint": "^10.9.1",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.24",
    "globals": "^16.5.0",
    "supabase": "2.116.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
